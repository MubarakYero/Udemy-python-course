# ip_add = input("Enter input here: ")
# segment = 1
# length = 0
# character = ''
# for character in ip_add:
#     if character == '.':
#         print("Segment {} contains {} characters".format(segment, length))
#         segment += 1
#         length = 0
#     else:
#         length += 1
# # unless the final character in the string is a. then we haven't printed the last segment
# if character != '.':
#     print("Segment {} contains {} characters".format(segment, length))
#
#
# Ty_pe = ("this is a very long string that i'm writing because i want to test "
#          "the script limit rule that python has for some reason")
